<%= form_for [@product, @review] do |f| %>
    <%= f.label "Post a new review ..." %><br/>
    <%= f.text_area :content %>
        <br>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :product_id, value: @product.id %>
    <%= f.submit 'Post review' %>
  </p>
<% end %>