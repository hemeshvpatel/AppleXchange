<%= form_for [@product, @review] do |f| %>
    <h2> <%= f.label "Post a new review ..." %> </h2>
    <%= f.text_area :content %>
        <br>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :product_id, value: @product.id %>
    <%= f.submit 'Submit' %>
  </p>
<% end %>