<%= form_for [@listing, @comment] do |f| %>
    <%= f.label "Post a new comment ..." %><br/>
    <%= f.text_area :content %>
        <br>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :listing_id, value: @listing.id %>
    <%= f.submit 'Post comment' %>
  </p>
<% end %>