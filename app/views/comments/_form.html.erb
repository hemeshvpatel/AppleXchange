<%= form_for [@listing, @comment] do |f| %>
    <h2> <%= f.label "Post a new comment:" %> </h2>
    <%= f.text_area :content, :rows => 3, style: 'width:100%;'%>
        <br>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :listing_id, value: @listing.id %>
    <%= f.submit 'Submit' %>
  </p>
<% end %>